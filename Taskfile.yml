# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: "3"

tasks:
  dev:
    cmds:
      - go run cmd/tui/main.go
  test:
    cmds:
      - go run cmd/tests/main.go
  make:
    cmds:
      - go build -o warbler ./cmd/tui/main.go && mv warbler ~/.local/bin/
